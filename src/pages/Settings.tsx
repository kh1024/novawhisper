import { ComingSoon } from "@/components/ComingSoon";
export default function Settings() {
  return <ComingSoon title="Settings" description="Data sources, AI provider, refresh interval, tape symbols, risk profile, secrets." />;
}
